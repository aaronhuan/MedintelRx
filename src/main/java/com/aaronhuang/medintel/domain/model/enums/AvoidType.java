package com.aaronhuang.medintel.domain.model.enums;

/**
 * Categories of items that can be avoided.
 */
public enum AvoidType {
    /** Avoid a food item. */
    FOOD,
    /** Avoid another medication. */
    MEDICATION
}
